# Development & Testing Guide\n\n## 🧪 Testing\n\n### Backend Testing\n\n#### Install Testing Dependencies\n```bash\ncd backend\nnpm install --save-dev jest supertest\n```\n\n#### Run Tests\n```bash\n# Run all tests\nnpm test\n\n# Run tests in watch mode\nnpm test -- --watch\n\n# Run tests with coverage\nnpm test -- --coverage\n```\n\n#### Sample Test File (backend/src/__tests__/auth.test.js)\n```javascript\nconst request = require('supertest');\nconst app = require('../server');\n\ndescribe('Authentication Routes', () => {\n  describe('POST /api/auth/login', () => {\n    it('should return error for invalid credentials', async () => {\n      const response = await request(app)\n        .post('/api/auth/login')\n        .send({\n          email: 'test@example.com',\n          password: 'wrongpassword'\n        });\n      \n      expect(response.status).toBe(401);\n      expect(response.body.success).toBe(false);\n    });\n  });\n});\n```\n\n### Frontend Testing\n\n#### Run Tests\n```bash\ncd frontend\nnpm test\n```\n\n#### Create Test File (frontend/src/__tests__/App.test.js)\n```javascript\nimport { render, screen } from '@testing-library/react';\nimport App from '../App';\n\ntest('renders app component', () => {\n  render(<App />);\n  const element = screen.getByText(/Barcode Management System/i);\n  expect(element).toBeInTheDocument();\n});\n```\n\n---\n\n## 🔧 Development Setup\n\n### Code Formatting\n\n#### Install Prettier\n```bash\nnpm install --save-dev prettier\n```\n\n#### Format Code\n```bash\n# Format all files\nnpm run format\n\n# Format specific file\nprettier --write src/App.js\n```\n\n### Linting\n\n#### Install ESLint\n```bash\nnpm install --save-dev eslint eslint-config-airbnb\n```\n\n#### Run Linter\n```bash\nnpm run lint\n```\n\n### Pre-commit Hooks\n\n#### Install Husky\n```bash\nnpm install husky --save-dev\nnpx husky install\n```\n\n#### Add Pre-commit Hook\n```bash\nnpx husky add .husky/pre-commit \"npm run lint && npm run format\"\n```\n\n---\n\n## 📚 API Testing with Postman\n\n### Import Postman Collection\n\n1. Download Postman: [postman.com](https://www.postman.com/downloads/)\n2. Create a new collection\n3. Add requests for each endpoint\n\n### Sample Requests\n\n#### Login\n```\nPOST http://localhost:5000/api/auth/login\nContent-Type: application/json\n\n{\n  \"email\": \"admin@example.com\",\n  \"password\": \"password123\"\n}\n```\n\n#### Get All Users\n```\nGET http://localhost:5000/api/users\nAuthorization: Bearer <token>\n```\n\n#### Create Employee\n```\nPOST http://localhost:5000/api/employees\nAuthorization: Bearer <token>\nContent-Type: application/json\n\n{\n  \"employeeId\": \"EMP001\",\n  \"name\": \"John Doe\",\n  \"email\": \"john@company.com\",\n  \"department\": \"IT\",\n  \"designation\": \"Developer\"\n}\n```\n\n#### Generate Barcode\n```\nPOST http://localhost:5000/api/barcodes/generate\nAuthorization: Bearer <token>\nContent-Type: application/json\n\n{\n  \"type\": \"employee\",\n  \"data\": {\n    \"employeeId\": \"EMP001\",\n    \"name\": \"John Doe\"\n  }\n}\n```\n\n---\n\n## 🐛 Debugging\n\n### Backend Debugging\n\n#### With Node Inspector\n```bash\ncd backend\nnode --inspect src/server.js\n```\n\nThen open `chrome://inspect` in Chrome to debug.\n\n#### Using VS Code Debugger\n\nCreate `.vscode/launch.json`:\n```json\n{\n  \"version\": \"0.2.0\",\n  \"configurations\": [\n    {\n      \"type\": \"node\",\n      \"request\": \"launch\",\n      \"name\": \"Launch Backend\",\n      \"program\": \"${workspaceFolder}/backend/src/server.js\",\n      \"restart\": true,\n      \"console\": \"integratedTerminal\"\n    }\n  ]\n}\n```\n\n### Frontend Debugging\n\n#### React Developer Tools\n1. Install [React Developer Tools](https://chrome.google.com/webstore/detail/react-developer-tools/) extension\n2. Open Chrome DevTools (F12)\n3. Go to Components tab to inspect React components\n\n#### Console Logging\n```javascript\nconsole.log('Debug message:', variable);\nconsole.error('Error:', error);\nconsole.table(arrayData);\n```\n\n---\n\n## 📊 Database Management\n\n### View Database in MySQL Workbench\n\n1. Open MySQL Workbench\n2. Create connection:\n   - Host: localhost\n   - Port: 3306\n   - User: root\n   - Password: (your password)\n3. Click on connection to browse database\n\n### Common Database Queries\n\n```sql\n-- View all tables\nSHOW TABLES;\n\n-- View table structure\nDESC users;\n\n-- View all data\nSELECT * FROM employees;\n\n-- Count records\nSELECT COUNT(*) FROM scans;\n\n-- Delete test data\nDELETE FROM employees WHERE employee_id LIKE 'TEST%';\n```\n\n### Database Backup\n\n```bash\n# Backup\nmysqldump -u root -p barcode_system > backup.sql\n\n# Restore\nmysql -u root -p barcode_system < backup.sql\n```\n\n---\n\n## 🚀 Performance Testing\n\n### Load Testing with Artillery\n\n#### Install\n```bash\nnpm install -g artillery\n```\n\n#### Create Test File (load-test.yml)\n```yaml\nconfig:\n  target: 'http://localhost:5000'\n  phases:\n    - duration: 60\n      arrivalRate: 10\n      name: \"Ramp up load\"\n\nscenarios:\n  - name: \"Health Check\"\n    flow:\n      - get:\n          url: \"/api/health\"\n```\n\n#### Run Test\n```bash\nartillery run load-test.yml\n```\n\n---\n\n## 📝 Development Workflow\n\n### Feature Branch Workflow\n\n```bash\n# Create feature branch\ngit checkout -b feature/new-feature\n\n# Make changes and commit\ngit add .\ngit commit -m \"Add new feature\"\n\n# Push to remote\ngit push origin feature/new-feature\n\n# Create Pull Request on GitHub\n# After review and approval, merge to main\n```\n\n### Commit Message Convention\n\n```\n<type>(<scope>): <subject>\n\n<body>\n\n<footer>\n```\n\nExamples:\n```\nfeat(auth): add JWT authentication\nfix(barcode): fix barcode generation error\ndocs(api): update API documentation\ntest(user): add user login tests\nrefactor(database): optimize queries\n```\n\n---\n\n## 🔒 Security Best Practices\n\n### Environment Variables\n- Never commit `.env` files\n- Use `.env.example` for templates\n- Rotate secrets regularly\n\n### Dependencies\n```bash\n# Check for vulnerabilities\nnpm audit\n\n# Update dependencies\nnpm update\n\n# Update major versions\nnpm outdated\n```\n\n### Code Review\n- Always review code before merge\n- Check for security issues\n- Test edge cases\n- Follow coding standards\n\n---\n\n**Happy Developing! 🎉**\n"
